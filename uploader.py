
# Takes a CSV list of  <instructor_email, class_id, start_date> 
# and uploads it to Neon CRM / google calendar

# Parse CSV


# Read event templates from Airtable 
# Prework: new airtable with templates (grab most recent of all events, unique by name)

# Match events by name

# Upload new events to Neon CRM (copying latest info but editing start date, dropping attendees and derived fields e.g. eventPage, adminPage URLs)
# https://developer.neoncrm.com/api/events/create-event-registration/

# Prework: Neon events -> Airtable -> Zapier -> Google Calendar automation
